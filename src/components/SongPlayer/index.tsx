import React, {Fragment} from "react";
import classNames from "classnames";
import styles from "./index.module.scss";

const SongPlayer = () => {
	return (
		<Fragment>
			<p className={styles.black}>this is music player</p>
		</Fragment>
	);
};

export default SongPlayer;
